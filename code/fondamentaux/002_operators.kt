// Kotlin dispose des opérateurs de base comme +, -, /, * et le modulo %

fun main () {
    // OPERATIONS ARITHMETIQUES +, -, *, /, %
    // OPERATIONS D'AFFECTATION ABBREGEES +=, -=, *=, /= 
    // OPERATEURS LOGIQUES &&, ||, !
    // OPERATEURS DE COMPARAISON ==, !=, >, <, <=, >=
    // OPERATION DE PLAGE OU D'APPARTENANCE .., in, !in
    // OPERATEUR DE TYPE is, as ex: varible is Type, val y = x as String // Effectue un cast
    // OPERATEUR D'INCREMENTATION ++, --
}